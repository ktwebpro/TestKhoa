@using Microsoft.AspNetCore.Identity
@inject SignInManager<AccountModel> SignInManager
@inject UserManager<AccountModel> UserManager

<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item">
        <a  class="nav-link text-dark" href="/Manage" title="Manage">Xin chào @User.Identity.Name!</a>
    </li>
    <li class="nav-item">
        <form  class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button  type="submit" class="nav-link btn btn-link text-dark">Thoát đăng nhập</button>
        </form>
    </li>
}
else
{
    <!--<li class="nav-item">-->
        @*<a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Register">Register</a>*@
        <!--<a class="nav-link text-dark" href="/Account/Register">Đăng ký</a>
    </li>-->
    <li class="nav-item">
        @*<a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Login">Login</a>*@
        <a class="nav-link text-dark" href="/Account/Login">Đăng nhập</a>
    </li>
}
</ul>
